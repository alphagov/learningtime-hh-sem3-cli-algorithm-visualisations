import chalk from 'chalk';

export const horizontalRule = () => {
  console.log(
    `${chalk.cyan(
      '----------------------------------------------------------------------------------------\n',
    )}`,
  );
};
